'use strict';

// A relatively complete development term, during which tasks may be accomplished.
// Think about the problems about delaying and multiple schedules.
export interface IDevTerms {
	name: string;
	from?: number;
	to?: number;
}
